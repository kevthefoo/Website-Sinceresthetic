import React from "react";
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "醫療團隊",
    description: "",
};

const Team: React.FC = () => {
    return <section>This is Team</section>;
};

export default Team;
