import React from 'react';
import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "診療項目",
    description: "",
};

const Services: React.FC = () => {
    return (
        <section>This is Services</section>
    );
}

export default Services;